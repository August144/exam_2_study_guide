package adapter;

public interface IPrimaryInterface {
  void cmdA();
  void cmdB(IPrimaryInterface comparator);
}

// LOL sorry <- i will never forgive u >:( <- :( <- :D
//but yeah i agree wit that it should wrap up and be much smoother
// that's the hope!